/** Cap rows returned from Supabase (upcoming filter applied server-side). */
export const WORKSHOP_MAX_UPCOMING_FETCH = 300

/** Workshops shown per page in list view before paging. */
export const WORKSHOP_LIST_PAGE_SIZE = 20
