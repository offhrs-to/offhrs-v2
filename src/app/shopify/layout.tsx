/** Nested layout — App Bridge is injected by root layout when x-offhrs-shopify-admin is set. */
export default function ShopifyAdminLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
